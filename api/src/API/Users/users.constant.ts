export enum Status {
    active = 'active',
    inactive = 'inactive',
}

export enum Role {
    user = 'user',
    admin = 'admin',
}

export enum Social {
    google = 'google',
    facebook = 'facebook',
}
